%h1 ユーザー一覧管理

%table.table.table-striped{'data-test': 'users__index'}
  %thead
    %tr
      %th.w-25 ID
      %th.w-25= t_model('shipping_address.full_name')
      %th.w-25= t_model('user.email')
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= user.id
        %td= user.full_name
        %td= user.email
        %td.btns
          = link_to '詳細', [:admin, user], class: 'btn btn-primary'
          = link_to '編集', edit_admin_user_path(user), class: 'btn btn-primary'
          = link_to '削除', [:admin, user], method: :delete, data: { confirm: '本当に実行しますか？' }, class: 'btn btn-danger'
